
define(['faceitConsApp' + window.__env.minUrl
    , window.__env.baseUrl + 'shared/sharedController' + window.__env.minUrl], function (app) {

        "use strict";

        app.controller("unusedController", ['$scope', function ($scope) {


        }]);

    });