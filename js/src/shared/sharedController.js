define(['faceitConsApp' + window.__env.minUrl
    , window.__env.baseUrl + 'shared/headerController' + window.__env.minUrl
    , window.__env.baseUrl + 'shared/footerController' + window.__env.minUrl], function (app) {

        "use strict";

        app.controller("sharedController", ['$scope', function ($scope) {


        }]);

    });
